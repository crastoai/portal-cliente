{
  "name": "portal-api",
  "version": "0.1.0",
  "private": true,
  "description": "Portal do Cliente Crasto.AI — middle-end (NestJS). O cliente nunca fala direto com o banco: tudo passa por aqui (JWT + RLS + regras).",
  "scripts": {
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.0",
    "@nestjs/core": "^10.4.0",
    "@nestjs/platform-express": "^10.4.0",
    "jose": "^5.9.0",
    "pg": "^8.12.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.0",
    "@types/node": "^22.5.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.5.4"
  }
}
