server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback — todas as rotas caem no index.html (React Router)
  location / {
    try_files $uri $uri/ /index.html;
  }

  # cache de assets com hash
  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
  }
}
